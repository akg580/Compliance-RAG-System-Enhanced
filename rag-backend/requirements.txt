# ComplianceAI RAG Backend — pinned production requirements
# Install:    pip install -r requirements.txt

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# LLM
groq==0.13.1

# Document processing
pypdf==5.1.0
pytesseract==0.3.13
Pillow==11.0.0

# Auth (bcrypt is optional — falls back to sha-256+salt if missing)
bcrypt==4.2.1

# Validation & config
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# HTTP client + tests
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0